# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=your_supabase_project_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# AI Service Keys (Optional - User-provided BYOK)
# Users can add their own API keys in the app settings
# These are NOT stored in the repository for security

# Agent Communication (For Claude Code and other AI agents)
# Generate a token in the app: Settings → Agent Tokens → Generate New Token
# Token format: agent_[12_random_chars]_[52_random_chars] (64 chars total)
# Example: agent_abc123def456_longrandomstringhere...
# Store this token securely - it will only be shown once
#
# Usage:
#   Authorization: Bearer <your_token>
#   Endpoint: https://[project].supabase.co/functions/v1/agent-read-note
#   Endpoint: https://[project].supabase.co/functions/v1/agent-update-note
#
# Security:
#   - Token expires after 90 days
#   - Auto-revokes after 10 failed authentication attempts
#   - Rate limited to 100 requests per hour
#   - Can be manually revoked anytime in app settings
#
NOTED_AGENT_TOKEN=agent_example_token_replace_with_real_token_from_app_settings
